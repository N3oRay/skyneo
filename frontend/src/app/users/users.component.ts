import { Component } from '@angular/core';

@Component({
  selector: 'app-users',
  template: `
    <div class="container my-4">
      <h2>Liste des utilisateurs (lazy loaded)</h2>
      <!-- Ajoute ici ton affichage dynamique -->
    </div>
  `
})
export class UsersComponent {}
